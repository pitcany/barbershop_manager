---
name: mongodb
description: |
  Designs MongoDB schemas, writes Motor async queries, and manages collections.
  Use when: adding/changing collections, implementing filters/aggregations, or tuning read/write patterns in backend services.
allowed-tools: Read, Edit, Write, Glob, Grep, Bash
---

# MongoDB Skill

Use this skill for backend database work:
- Motor async queries and projections
- Aggregations and pagination patterns
- Collection/schema conventions
- Safe updates and data consistency checks

Follow project rules like UUID string IDs and excluding `_id` from API responses.
